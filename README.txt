# poetry-ebook-formatting
HTML/CSS code for ensuring proper display of verse/poetry text on web browser and e-reader screens.

The example HTML doc shows how to format the poem text, with each verse contained within a <div> element and each line consisting of a separate <p> element. The CSS stylesheet styles the text so that a line will indent whenever it overflows the width of the screen or browser window. 
